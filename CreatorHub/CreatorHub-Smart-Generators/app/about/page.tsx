import type { Metadata } from "next";
import { PageShell } from "@/components/ui";
export const metadata: Metadata={title:"About CreatorHub",description:"Learn what CreatorHub is building for creators and small businesses."};
export default function About(){return <PageShell><div className="container"><div className="page-header"><span className="eyebrow">ABOUT</span><h1>CreatorHub is a practical AI workspace for creators.</h1><p>CreatorHub brings lightweight content tools, reusable prompts and AI-tool discovery into one simple place.</p></div><div className="content-prose"><h2>The idea</h2><p>The first version is deliberately simple: useful tools that run in the browser, a prompt library, a sample AI directory and original learning resources.</p><h2>Why it stays free</h2><p>The MVP avoids paid AI APIs and accounts so the core experience is accessible. Future monetization can be layered on without blocking basic tool usage.</p><h2>Built for iteration</h2><p>The architecture keeps branding, tool definitions and future integrations separate, making it easier to rename CreatorHub, connect APIs later, or add analytics and payments when the product is ready.</p></div></div></PageShell>}
